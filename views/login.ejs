<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In - Campus Carpool Connect</title>
    <link rel="stylesheet" href="login.css">
</head>
<body>
    <div class="login-page">
        <!-- Navbar -->
        <nav class="navbar">
            <div class="navbar-logo">Campus Carpool Connect</div>
            <div class="navbar-links">
                <a href="/">Home</a>
                <a href="/signUp">Sign Up</a>
            </div>
        </nav>

        <!-- Login header -->
        <header class="login-header">
            <h1>Log In</h1>
            <p>Access your account by providing your roll number and password.</p>
        </header>

        <!-- Login form section -->
        <section class="login-form-section">
            <form class="login-form" action="/logindetails" method="POST">
                <div class="form-group">
                    <label for="rollNo">Roll Number:</label>
                    <input type="text" id="rollNo" name="rollNo" required>
                </div>
                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <button type="submit" class="cta-button">Log In</button>
            </form>
        </section>

        <!-- Footer -->
        <%- include('components/footer.ejs') %>
    </div>
</body>
</html>
