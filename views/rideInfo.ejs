<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ride Information - Campus Carpool Connect</title>
    <style>
        /* Keeping original styles for body, navbar, and footer */
        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f0f4f8;
            margin: 0;
            padding: 0;
        }
        /* Navbar */
.navbar {
    background-color: #6c63ff;
    padding: 15px 5%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-radius: 0 0 20px 20px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    /* font-size: 18px; */
}

.navbar-logo {
    font-size: 1.8em;
    color: white;
    font-weight: bold;
}

.navbar-links {
    display: flex;
    align-items: center;
}

.navbar-links a {
    margin-left: 20px;
    color: white;
    font-size: 1.1em;
    padding: 8px 15px;
    border-radius: 20px;
    transition: all 0.3s ease;
}

.navbar-links a:hover {
    background-color: #5a52d5;
    transform: translateY(-2px);
}



        .ride-info-page {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Enhanced Ride Information Section */
        .ride-info-section {
            flex: 1;
            max-width: 900px;
            margin: 40px auto;
            padding: 40px;
            background-color: #ffffff;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(108, 99, 255, 0.1);
        }

        .ride-info-section h1 {
            color: #6c63ff;
            font-size: 2.5em;
            margin-bottom: 40px;
            text-align: center;
            position: relative;
        }

        .ride-info-section h1::after {
            content: '';
            display: block;
            width: 100px;
            height: 4px;
            background: linear-gradient(90deg, #6c63ff, #ff6464);
            margin: 10px auto 0;
            border-radius: 2px;
        }

        .info-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
        }

        .driver-details, .ride-details {
            background-color: #f8f9ff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }

        .details-header {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }

        .details-header h2 {
            color: #6c63ff;
            font-size: 1.8em;
            margin: 0;
            flex-grow: 1;
        }

        .info-item {
            display: flex;
            margin-bottom: 15px;
            align-items: center;
        }

        .info-label {
            font-weight: 600;
            color: #666;
            width: 40%;
            margin-right: 10px;
        }

        .info-value {
            color: #333;
            font-weight: 500;
            flex-grow: 1;
        }

        /* Enhanced Form Styles */
        .booking-section {
            margin-top: 40px;
            text-align: center;
        }

        .seat-selection {
            margin-bottom: 30px;
        }

        label {
            display: block;
            margin-bottom: 10px;
            font-weight: 600;
            color: #6c63ff;
        }

        select {
            width: 100%;
            max-width: 400px;
            padding: 12px;
            border: 2px solid #6c63ff;
            border-radius: 10px;
            font-size: 1.1em;
            background-color: #f8f9ff;
            transition: all 0.3s ease;
        }

        select:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(108, 99, 255, 0.3);
        }

        .button-group {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }

        .book-button, .view-profile-button {
            padding: 15px 40px;
            border: none;
            border-radius: 30px;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .book-button {
            background-color: #6c63ff;
            color: white;
        }

        .view-profile-button {
            background-color: #ff6464;
            color: white;
        }

        .book-button:hover, .view-profile-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        /* Footer */
.footer {
    padding: 30px 5%;
    background-color: #6c63ff;
    color: white;
    text-align: center;
    margin-top: auto;
    border-radius: 50px 50px 0 0;
}

.social-media-links {
    margin: 20px 0;
}

.social-media-links a {
    margin: 0 15px;
    color: white;
    font-size: 1.3em;
    transition: all 0.3s ease;
}

.social-media-links a:hover {
    color: #ff6464;
    transform: translateY(-3px);
}


        /* Responsive Design */
        @media (max-width: 768px) {
            .info-container {
                grid-template-columns: 1fr;
            }

            .ride-info-section {
                padding: 20px;
                margin: 20px;
            }

            .button-group {
                flex-direction: column;
            }

            .book-button, .view-profile-button {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="ride-info-page">
        <%- include('components/navbar.ejs') %>

        <section class="ride-info-section">
            <h1>Ride Information</h1>
            
            <div class="info-container">
                <div class="driver-details">
                    <div class="details-header">
                        <h2>Car Owner Details</h2>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Roll Number:</span>
                        <span class="info-value"><%= ride.rollno %></span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Name:</span>
                        <span class="info-value"><%= ride.name %></span>
                    </div>
                </div>
                
                <div class="ride-details">
                    <div class="details-header">
                        <h2>Ride Details</h2>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Destination:</span>
                        <span class="info-value"><%= ride.destination %></span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Start Time:</span>
                        <span class="info-value"><%= ride.starttime %></span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Duration:</span>
                        <span class="info-value"><%= ride.duration %></span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Car Name:</span>
                        <span class="info-value"><%= ride.carname %></span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Available Seats:</span>
                        <span class="info-value"><%= ride.seatnum %></span>
                    </div>
                </div>
            </div>

            <div class="booking-section">
                <form action="/bookRide" method="GET" onsubmit="return setSelectedPosition();">
                    <div class="seat-selection">
                        <label for="positionSelect">Select Your Seat Position</label>
                        <select name="selectedPosition" id="positionSelect" required>
                            <% ride.pos.split(',').forEach(function(position) { %>
                                <option value="<%= position.trim() %>"><%= position.trim() %></option>
                            <% }); %>
                        </select>
                    </div>
                    
                    <input type="hidden" name="rideId" value="<%= ride.rideid %>">
                    <input type="hidden" name="seatNum" value="<%= ride.seatnum %>">
                    <input type="hidden" id="selectedPosition" name="selectedPosition" required>
                    
                    <div class="button-group">
                        <button type="submit" class="book-button">Book This Ride</button>
                        <a href="/viewProfile?rollno=<%= ride.rollno %>">
                            <button type="button" class="view-profile-button">View Rider Profile</button>
                        </a>
                    </div>
                </form>
            </div>
        </section>

        <%- include('components/footer.ejs') %>
    </div>

    <script>
        function setSelectedPosition() {
            const positionSelect = document.getElementById('positionSelect');
            const selectedPositionInput = document.getElementById('selectedPosition');
            selectedPositionInput.value = positionSelect.value;
            return true;
        }
    </script>
</body>
</html>